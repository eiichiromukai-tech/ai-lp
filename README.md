# RE/MAX COMPASS 物件ポータル

商業不動産・店舗仲介を専門とする **RE/MAX COMPASS（株式会社ロメッツ）** の事業用不動産ポータルサイト。
店舗・オフィス・倉庫・一棟ビル・事業用地を、エリア／賃料／面積／駅徒歩／こだわり条件で検索できます。

> **重要**
> - 掲載している **物件情報はすべてデモ用のサンプルデータ** です。実在の募集物件ではありません。実運用の前に `data/properties.js` を実データに差し替えてください（全ページに注意書きバナーを表示しています）。
> - **会社概要（所在地・電話番号・宅建免許番号等）は実在する情報** を掲載しています。公開前に必ず内容の正確性をご確認ください。
> - お問い合わせフォームは **フロントエンドのみの実装** で、送信先は未接続です（バリデーション・送信中表示までは動作します）。

以前の1枚もののランディングページは本ポータルに置き換えました（内容は git 履歴に残っています）。

## 技術構成

- 素の HTML / CSS / JavaScript（ビルド不要・依存パッケージなし）
- 物件データは `data/properties.js` にグローバル変数として定義（`file://` でも動作します）
- フォント: Google Fonts（Montserrat, Zen Kaku Gothic New）

## ファイル構成

```
index.html          … トップ（ヒーロー検索・種別/エリア別導線・新着物件・最近見た物件）
properties.html     … 物件検索（絞り込み・並び替え・ページング）
property.html       … 物件詳細（?id=CMP-1001 形式）
favorites.html      … お気に入り一覧
contact.html        … お問い合わせ・物件リクエスト＋会社概要
owner.html          … 物件オーナー様向け（リーシング・掲載までの流れ）

css/style.css       … スタイル一式（デザイントークン + 全ページ分）
data/properties.js  … 物件データ（サンプル24件）・種別/エリア/こだわり条件のマスタ
js/portal.js        … 共通ロジック（整形・検索・お気に入り・画像生成・ヘッダー）
js/home.js          … トップページ
js/search.js        … 検索ページ
js/detail.js        … 詳細ページ
js/favorites.js     … お気に入りページ
js/contact.js       … お問い合わせフォーム
images/             … ロゴ・ブランド素材
```

## 主な機能

### 検索・絞り込み（`properties.html`）

- **絞り込み条件**: フリーワード / 物件種別 / エリア（区） / 月額賃料レンジ / 面積レンジ（坪） / 駅徒歩 / 募集状況 / こだわり条件（居抜き・1階路面・セットアップ 等）
- **成約済の物件は検索結果に出ません**（「成約済」を明示的に選んだときのみ表示）。詳細ページは残るため、過去の取扱実績としてURLを共有できます
- **並び替え**: 新着順・賃料の安い/高い順・面積の広い/狭い順・坪単価の安い順
- **ページング**: 1ページ9件
- 条件は **URLクエリと同期** するため、検索結果をそのまま共有・ブックマークできます（例: `properties.html?type=shop&area=千代田区&feature=居抜き`）
- 選択中の条件はチップで表示し、クリックで個別解除
- 各チェックボックスに該当件数を表示、絞り込み結果は入力と同時に更新
- 1024px以下では絞り込みパネルがドロワーに切り替わります

### 物件詳細（`property.html?id=...`）

物件概要テーブル（賃料・共益費・坪単価・敷金・面積・階数・構造・築年・用途・入居時期・取引態様・更新日）、この物件についての説明、契約までの流れ、物件個別のお問い合わせフォーム、類似物件のレコメンド。ページタイトルと meta description は物件情報から動的に生成します。

### お気に入り・閲覧履歴

`localStorage` に保存します（サーバー送信なし）。

- 物件カードと詳細ページのボタンから登録／解除、ヘッダーに件数バッジを表示
- `favorites.html` で一覧・一括削除
- お問い合わせフォームに、お気に入り登録中の物件を自動で添付表示
- トップページに「最近見た物件」（直近8件を保持し3件表示）

### 物件画像について

現地写真は未登録のため、**物件種別とIDから決定的に生成する SVG プレースホルダ** を表示しています（`js/portal.js` の `placeholderImage()`）。RE/MAX公式カラーを使った4種のパレットで、店舗・オフィス・倉庫・一棟ビル・事業用地それぞれの図版を描き分けます。詳細ページには「画像は物件種別に基づくイメージ」である旨を明記しています。

実写真を使う場合は、物件データに画像パスの配列を追加し、`placeholderImage()` の呼び出し箇所をその画像に差し替えてください。

## デザインシステム

- **カラー**: RE/MAX公式パレットを `css/style.css` の `:root` にCSS変数として厳密値で定義（Red `#FF1200` / Blue `#0043FF` / Cream `#F7F5EE` / Black / White / Dark Red `#650000` / Dark Blue `#000B35` / Bridge Red・Blue / Sky Blue / Dark Charcoal Gray）。背景は白・クリーム・濃紺中心、赤をCTA・賃料表示のアクセントとして使用
- **フォント**: Montserrat（見出し・数字）+ Zen Kaku Gothic New（日本語本文）
- **レイアウト**: 余白とヘアラインで区切る編集的なレイアウト、左寄せ見出し、ダッシュ＋テキストの控えめなCTA（`.link-cta`）
- **コントラスト対応**: 赤地に白文字は基準（4.5:1）未達のため、赤ボタンは黒文字（5.3:1）、ホバー時は濃赤地に白文字へ切り替え
- **アクセシビリティ**: スキップリンク、`aria-live` による検索結果件数の通知、フォーカスリング、`prefers-reduced-motion` 対応、フォームのリアルタイムバリデーションとエラーの `aria-invalid` 連動

## 物件データの追加・編集

`data/properties.js` の `PROPERTIES` 配列に追記します。`areaSqm`（m²換算）と `tsuboUnitPrice`（坪単価）はファイル末尾で自動計算されるため、記述不要です。

```js
{
  id: 'CMP-1025',                 // 物件番号（URLのキーになります）
  title: '物件名',
  type: 'shop',                   // shop | office | warehouse | building | land
  status: 'available',            // available（募集中）| negotiating（商談中）| closed（成約済）
  ward: '渋谷区',                  // AREAS 配列にある区名
  address: '東京都渋谷区〇〇一丁目',
  access: [{ line: 'JR山手線', station: '渋谷', walk: 5 }],
  rent: 500000,                   // 月額賃料（円）0 で「応相談」表示
  managementFee: 40000,           // 共益費（円／月）
  deposit: 10,                    // 敷金・保証金（ヶ月）
  keyMoney: 0,                    // 礼金（ヶ月）
  areaTsubo: 25.0,
  floor: '1F',
  floorsTotal: 6,
  builtYear: 2005,
  structure: 'RC造',
  features: ['1階路面', '飲食可'],  // FEATURES 配列の値
  usage: ['飲食店', '物販'],
  availableFrom: '即入居可',
  updatedAt: '2026-08-01',        // 新着バッジの自動判定と新着順の並び替えに使用
  description: '物件の説明文。'
}
```

エリアやこだわり条件の選択肢を増やす場合は、同ファイル冒頭の `AREAS` / `FEATURES` / `PROPERTY_TYPES` に追加してください（検索画面のチェックボックスは、該当件数が1件以上ある項目のみ自動表示されます）。

### 募集状況の運用

物件データが持つ `status` は **募集中 / 商談中 / 成約済** の3つです。「新着」はデータ側で持たず、`updatedAt` から自動判定します（`js/portal.js` の `NEW_DAYS`、既定14日）。掲載日から14日以内なら新着バッジが付き、期間を過ぎると自動的に「募集中」に変わるため、外し忘れは起きません。商談中・成約済の物件には新着バッジは付きません。

成約したら `status: 'closed'` に変更します。検索結果・トップページ・種別/エリアの件数・類似物件から自動的に除外され、詳細ページだけが残ります（成約済の告知と「同じ条件の物件を探す」導線に切り替わり、問い合わせフォームは非表示になります）。データごと削除してしまっても表示は壊れませんが、実績として残す場合は `closed` を使ってください。

### 日付の扱い（サンプルデータ）

サンプルデータでは `updatedAt` 未設定の物件に、掲載順で5日ずつ古い日付を自動割り当てしています。日付ベースで新着を判定するため、**サンプルのまま時間が経つと新着バッジは自然に消えます**。実データでは各物件に必ず `updatedAt` を設定してください。

## ローカルでの確認

```bash
python3 -m http.server 8000
# http://localhost:8000/ を開く
```

`index.html` をブラウザで直接開いても動作します（データをJSONではなくJSファイルで保持しているため）。

## 今後の実装候補

- お問い合わせフォームの送信先接続（フォームAPI／メール送信）
- 物件の実写真・図面の登録
- 地図表示（Google Maps / 国土地理院タイル等）と沿線・駅からの検索
- 物件データのCMS化（マイクロCMS等）または管理画面の追加
- 各物件ページの構造化データ（JSON-LD）出力とサイトマップ生成
